#include "main.h"

/**
 * add - Take two parameters to add two integers
 * @x: int type number 1
 * @y: int type number 2
 * Return: return the addition
 */
int add(int x, int y)
{
	return (x + y);
}
